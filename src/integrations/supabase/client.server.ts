// Lovable Cloud manages the service role key internally.
// Admin operations that require bypassing RLS must use Lovable Cloud
// server functions (createServerFn) — never construct the admin client manually.
// This file is kept as a stub so existing imports don't break.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const supabaseAdmin: any = null; // Not used — see deleteUser.functions.ts
